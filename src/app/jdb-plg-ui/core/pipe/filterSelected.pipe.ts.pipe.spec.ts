/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterSelected.pipe.tsPipe } from './filterSelected.pipe.ts.pipe';

describe('Pipe: FilterSelected.pipe.tse', () => {
  it('create an instance', () => {
    let pipe = new FilterSelected.pipe.tsPipe();
    expect(pipe).toBeTruthy();
  });
});